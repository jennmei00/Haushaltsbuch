import 'package:flutter/material.dart';

class Kreisdiagramm extends StatelessWidget {
  const Kreisdiagramm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
