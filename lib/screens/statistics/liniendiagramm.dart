import 'package:flutter/material.dart';

class Liniendiagramm extends StatelessWidget {
  const Liniendiagramm({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}